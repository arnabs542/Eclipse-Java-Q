package queueStackRelated;


//This implementation in QueueByFixedSizeArray uses size to record the status of the queue
//If not recoding the size, we can do this:
//(head, tail), only the elements between head and tail is elements in queue
//== Initial == 
//0     1    2   3
//head  tail
//head + 1 == tail -> empty
//head == tail -> full

public class QueueByFixedSizeArrayII {

}



